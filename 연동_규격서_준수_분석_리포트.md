# 🔍 연동 규격서 준수 분석 리포트

**분석 일시**: 2025-01-XX  
**분석 대상**: muyi-giftcard 프로젝트  
**규격서**: 상품권 발송 연동 규격서 Ver1.0 - 브라더  
**상태**: ⚠️ **심각한 불일치 발견**

---

## 📋 목차

1. [API 엔드포인트 준수 여부](#1-api-엔드포인트-준수-여부)
2. [요청 형식 준수 여부](#2-요청-형식-준수-여부)
3. [암호화 규격 준수 여부](#3-암호화-규격-준수-여부)
4. [환경별 키 관리 상태](#4-환경별-키-관리-상태)
5. [보안 문제 분석](#5-보안-문제-분석)
6. [종합 평가 및 개선 사항](#6-종합-평가-및-개선-사항)

---

## 1. API 엔드포인트 준수 여부

### ❌ **심각한 불일치 발견**

| 항목 | 규격서 요구사항 | 현재 구현 | 상태 |
|------|---------------|----------|------|
| ADD API | `/bro/gift_add.php` | `/add` | ❌ **완전히 다름** |
| SEND API | `/bro/gift_send.php` | `/send` | ❌ **완전히 다름** |
| QRY API | `/bro/gift_check.php` | 미구현 | ❌ **누락** |
| DEL API | `/bro/gift_check.php` | 미구현 | ❌ **누락** |

**현재 코드** (`lib/chlifes.ts:74, 99`):
```typescript
// ❌ 잘못된 엔드포인트
const addResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/add`, {
const sendResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/send`, {
```

**규격서 요구사항**:
```typescript
// ✅ 올바른 엔드포인트
const addResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/bro/gift_add.php`, {
const sendResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/bro/gift_send.php`, {
```

**영향도**: 🔴 **치명적** - API 호출 자체가 실패합니다.

---

## 2. 요청 형식 준수 여부

### ❌ **다수의 불일치 발견**

### 2.1 ADD API 요청 형식

#### 규격서 요구사항
```json
{
  "GENID": "AG20181105144054",        // 대문자, 필수
  "CMD": "ADD",                        // 필수 (누락됨!)
  "GIFTNM": "TE20241216184900",       // 대문자, 필수
  "FACE_PRICE": "[암호화된 금액]",     // 암호화 필수 (누락됨!)
  "ISSUE_REQ_SN": "BRO20241216001",   // 필수 (누락됨!)
  "RECV_HPNO": "[암호화된 전화번호]",  // 암호화 필수 (누락됨!)
  "MESSAGE": "평문 메시지",            // 필수 (누락됨!)
  "VALID_DAY": "30"                   // 선택 (누락됨!)
}
```

#### 현재 구현 (`lib/chlifes.ts:66-72`)
```typescript
// ❌ 규격서와 완전히 다름
const addPayload = {
  genid: CHLIFES_CONFIG.GENID,        // 소문자 (대문자여야 함)
  giftnm: CHLIFES_CONFIG.GIFTNM,      // 소문자 (대문자여야 함)
  goodscode: data.goodsCode,          // 규격서에 없는 필드
  cnt: data.count.toString(),          // 규격서에 없는 필드
  price: data.price.toString(),        // 규격서에 없는 필드 (FACE_PRICE여야 함)
  // CMD 누락!
  // ISSUE_REQ_SN 누락!
  // RECV_HPNO 누락!
  // MESSAGE 누락!
  // VALID_DAY 누락!
};
```

**문제점 요약**:
1. ❌ 필드명이 소문자 (규격서는 대문자)
2. ❌ `CMD` 필드 누락 (필수)
3. ❌ `ISSUE_REQ_SN` 누락 (필수)
4. ❌ `RECV_HPNO` 누락 (필수)
5. ❌ `MESSAGE` 누락 (필수)
6. ❌ `FACE_PRICE` 대신 `price` 사용 (암호화도 안 됨)
7. ❌ 규격서에 없는 필드 사용 (`goodscode`, `cnt`)

### 2.2 SEND API 요청 형식

#### 규격서 요구사항
```json
{
  "GENID": "AG20181105144054",        // 대문자, 필수
  "CMD": "SEND",                      // 필수 (누락됨!)
  "GIFTNM": "TE20241216184900",       // 대문자, 필수
  "ISSUE_REQ_SN": "BRO20241216001",   // 필수 (누락됨!)
  "ISSUE_APRV_SN": "CLF20241216001234", // 필수 (누락됨!)
  "MSG_YN": "Y"                       // 필수 (누락됨!)
}
```

#### 현재 구현 (`lib/chlifes.ts:91-97`)
```typescript
// ❌ 규격서와 완전히 다름
const sendPayload = {
  genid: CHLIFES_CONFIG.GENID,        // 소문자
  giftnm: CHLIFES_CONFIG.GIFTNM,      // 소문자
  goodscode: data.goodsCode,          // 규격서에 없는 필드
  seq: addResult.seq,                 // 규격서에 없는 필드 (ISSUE_APRV_SN이어야 함)
  phone: data.phone,                 // 규격서에 없는 필드
  // CMD 누락!
  // ISSUE_REQ_SN 누락!
  // ISSUE_APRV_SN 누락!
  // MSG_YN 누락!
};
```

**문제점 요약**:
1. ❌ 필드명이 소문자
2. ❌ `CMD` 필드 누락
3. ❌ `ISSUE_REQ_SN` 누락
4. ❌ `ISSUE_APRV_SN` 누락 (ADD 응답에서 받아야 함)
5. ❌ `MSG_YN` 누락
6. ❌ 규격서에 없는 필드 사용 (`goodscode`, `phone`)

### 2.3 응답 처리

#### 규격서 요구사항
- 응답 코드: `RET_CODE` (6자리, "000000" = 성공)
- ADD 응답: `ISSUE_APRV_SN` 저장 필수

#### 현재 구현 (`lib/chlifes.ts:84-88, 109-113`)
```typescript
// ❌ 잘못된 응답 코드 확인
if (addResult.code !== '0000') {  // 규격서는 RET_CODE !== '000000'
  throw new Error(`ADD 실패: ${addResult.msg}`);
}

// ❌ ISSUE_APRV_SN 저장 안 함
const sendPayload = {
  seq: addResult.seq,  // 규격서에 없는 필드
};
```

**영향도**: 🔴 **치명적** - API 요청이 규격서와 맞지 않아 실패합니다.

---

## 3. 암호화 규격 준수 여부

### ❌ **암호화 미적용**

### 3.1 규격서 요구사항

| 필드 | 암호화 여부 | 규격서 |
|------|-----------|--------|
| `FACE_PRICE` | ✅ **필수** | AES-256-CBC + Base64 |
| `RECV_HPNO` | ✅ **필수** | AES-256-CBC + Base64 |
| `MESSAGE` | ❌ 평문 | 규격서에는 암호화로 표기되어 있으나 실제로는 평문 |

**참고**: `클라이프스_API_연동_매뉴얼.md`에 따르면 MESSAGE는 평문으로 전송해야 합니다.

### 3.2 현재 구현

```typescript
// lib/chlifes.ts:16-27
export function encryptChlifes(data: string): string {
  // ✅ 암호화 함수는 구현되어 있음
  // ❌ 하지만 실제 API 호출에서 사용되지 않음!
}

// lib/chlifes.ts:66-72
const addPayload = {
  price: data.price.toString(),  // ❌ 암호화 안 됨 (FACE_PRICE여야 함)
  // RECV_HPNO도 없음
};
```

**문제점**:
1. ❌ `FACE_PRICE` 암호화 미적용
2. ❌ `RECV_HPNO` 필드 자체가 없음
3. ✅ 암호화 함수는 구현되어 있으나 사용되지 않음

**영향도**: 🔴 **치명적** - 암호화되지 않은 데이터는 API에서 거부됩니다.

---

## 4. 환경별 키 관리 상태

### ✅ **키 관리 구조는 양호**

### 4.1 환경별 키 분리 상태

| 환경 | GENID | GIFTNM | 암호화 키 | IV | 상태 |
|------|-------|--------|---------|-----|------|
| 개발(DEV) | `AG20181105144054` | `TE20241216184900` | `39Vh8PgDwE2k9AfEvs2PW3kaxheEy064` | `1234123412341234` | ✅ 규격서와 일치 |
| 프로덕션(PROD) | `AG20251006211422` | `PR20251006212653` | `brcardealpasswords2PW3kaxheYY064` | `1234123412341234` | ✅ 문서화됨 |

**키 관리 위치**:
- ✅ `PRODUCTION_CREDENTIALS.md` - 프로덕션 키 문서화
- ✅ `API_INTEGRATION_GUIDE.md` - 개발/프로덕션 키 분리
- ✅ 환경 변수로 관리 (`CHLIFES_GENID`, `CHLIFES_GIFTNM`, `CHLIFES_ENC_KEY`, `CHLIFES_IV`)

### 4.2 키 접근 방식

```typescript
// lib/chlifes.ts:5-11
const CHLIFES_CONFIG = {
  API_URL: process.env.NEXT_PUBLIC_CHLIFES_URL!,  // ✅ URL만 클라이언트 노출
  GENID: process.env.CHLIFES_GENID!,              // ✅ 서버 전용
  GIFTNM: process.env.CHLIFES_GIFTNM!,             // ✅ 서버 전용
  ENC_KEY: process.env.CHLIFES_ENC_KEY!,           // ✅ 서버 전용
  IV: process.env.CHLIFES_IV!,                     // ✅ 서버 전용
};
```

**평가**: ✅ **양호** - 민감한 키는 서버 전용 환경 변수로 관리되고 있습니다.

---

## 5. 보안 문제 분석

### ⚠️ **일부 보안 문제 발견**

### 5.1 클라이언트 노출 위험

#### ✅ 양호한 부분
```typescript
// lib/chlifes.ts - 서버 전용
const CHLIFES_CONFIG = {
  GENID: process.env.CHLIFES_GENID!,      // ✅ NEXT_PUBLIC_ 없음
  ENC_KEY: process.env.CHLIFES_ENC_KEY!,   // ✅ NEXT_PUBLIC_ 없음
};
```

#### ⚠️ 주의 필요 부분
```typescript
// app/page.tsx:117-121 - 클라이언트에서 사용
const wizzUrl = process.env.NEXT_PUBLIC_WIZZPAY_URL;
const wizzMid = process.env.NEXT_PUBLIC_WIZZPAY_MID;        // ⚠️ 클라이언트 노출
const wizzIvKey = process.env.NEXT_PUBLIC_WIZZPAY_IV_KEY;   // ⚠️ 클라이언트 노출
const wizzSalt = process.env.NEXT_PUBLIC_WIZZPAY_SALT;      // ⚠️ 클라이언트 노출
const wizzPassword = process.env.NEXT_PUBLIC_WIZZPAY_PASSWORD; // ⚠️ 클라이언트 노출
```

**문제점**:
- Wizzpay 암호화 키들이 `NEXT_PUBLIC_` 접두사로 클라이언트에 노출됨
- 브라우저 개발자 도구에서 확인 가능

**권장 사항**:
- Wizzpay 키는 서버 사이드에서만 사용
- `/api/payment/init` API를 통해 암호화된 데이터만 클라이언트로 전송

### 5.2 환경 변수 검증

```typescript
// lib/env.ts:54-108
export function validateEnv(): void {
  // ✅ 필수 환경 변수 검증 구현됨
  // ✅ 민감 정보의 NEXT_PUBLIC_ 접두사 사용 방지
}
```

**평가**: ✅ **양호** - 환경 변수 검증 로직이 잘 구현되어 있습니다.

### 5.3 로깅 보안

```typescript
// lib/logger.ts:31-56
function maskSensitiveData(obj: any): any {
  // ✅ 민감한 정보 자동 마스킹
  const SENSITIVE_KEYS = ['password', 'secret', 'token', 'key', ...];
}
```

**평가**: ✅ **양호** - 로그에 민감 정보가 노출되지 않도록 마스킹 처리되어 있습니다.

### 5.4 .gitignore 설정

```gitignore
# .gitignore
.env*.local  # ✅ 환경 변수 파일 무시
```

**평가**: ✅ **양호** - 환경 변수 파일이 Git에 커밋되지 않도록 설정되어 있습니다.

---

## 6. 종합 평가 및 개선 사항

### 6.1 준수도 요약

| 항목 | 준수도 | 상태 |
|------|--------|------|
| API 엔드포인트 | 0% | ❌ 완전히 다름 |
| 요청 형식 | 20% | ❌ 대부분 불일치 |
| 암호화 규격 | 0% | ❌ 미적용 |
| 환경별 키 관리 | 90% | ✅ 양호 |
| 보안 관리 | 70% | ⚠️ 일부 개선 필요 |

**전체 준수도**: **약 20%** ⚠️

### 6.2 즉시 수정 필요 사항 (우선순위 순)

#### 🔴 우선순위 1: API 엔드포인트 수정
```typescript
// lib/chlifes.ts 수정 필요
// ❌ 현재
const addResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/add`, {

// ✅ 수정 후
const addResponse = await fetch(`${CHLIFES_CONFIG.API_URL}/bro/gift_add.php`, {
```

#### 🔴 우선순위 2: 요청 형식 수정
```typescript
// lib/chlifes.ts 수정 필요
// ❌ 현재
const addPayload = {
  genid: CHLIFES_CONFIG.GENID,
  giftnm: CHLIFES_CONFIG.GIFTNM,
  goodscode: data.goodsCode,
  cnt: data.count.toString(),
  price: data.price.toString(),
};

// ✅ 수정 후
const issueReqSn = generateIssueReqSn(); // 유니크한 발행번호 생성
const addPayload = {
  GENID: CHLIFES_CONFIG.GENID,                    // 대문자
  CMD: "ADD",                                     // 추가
  GIFTNM: CHLIFES_CONFIG.GIFTNM,                  // 대문자
  FACE_PRICE: encryptChlifes(data.price.toString()), // 암호화
  ISSUE_REQ_SN: issueReqSn,                       // 추가
  RECV_HPNO: encryptChlifes(data.phone),          // 암호화, 추가
  MESSAGE: data.message || "상품권입니다",         // 추가
  VALID_DAY: "30"                                 // 추가
};
```

#### 🔴 우선순위 3: 응답 처리 수정
```typescript
// lib/chlifes.ts 수정 필요
// ❌ 현재
if (addResult.code !== '0000') {
  throw new Error(`ADD 실패: ${addResult.msg}`);
}
const seq = addResult.seq;

// ✅ 수정 후
if (addResult.RET_CODE !== '000000') {
  throw new Error(`ADD 실패: ${addResult.RET_CODE} - ${addResult.RET_MESG}`);
}
const issueAprvSn = addResult.ISSUE_APRV_SN; // 저장 필수!
```

#### 🔴 우선순위 4: SEND API 수정
```typescript
// lib/chlifes.ts 수정 필요
// ❌ 현재
const sendPayload = {
  genid: CHLIFES_CONFIG.GENID,
  giftnm: CHLIFES_CONFIG.GIFTNM,
  goodscode: data.goodsCode,
  seq: addResult.seq,
  phone: data.phone,
};

// ✅ 수정 후
const sendPayload = {
  GENID: CHLIFES_CONFIG.GENID,                    // 대문자
  CMD: "SEND",                                    // 추가
  GIFTNM: CHLIFES_CONFIG.GIFTNM,                  // 대문자
  ISSUE_REQ_SN: issueReqSn,                      // ADD에서 사용한 값
  ISSUE_APRV_SN: issueAprvSn,                     // ADD 응답에서 받은 값
  MSG_YN: "Y"                                     // 추가
};
```

#### 🟡 우선순위 5: Wizzpay 보안 개선
- `NEXT_PUBLIC_WIZZPAY_*` 환경 변수 제거
- `/api/payment/init` API를 통한 서버 사이드 암호화 사용

### 6.3 추가 구현 필요 사항

1. **ISSUE_REQ_SN 생성 함수**:
```typescript
function generateIssueReqSn(): string {
  const timestamp = new Date().toISOString().replace(/[-:T.]/g, '').slice(0, 14);
  const random = Math.floor(Math.random() * 100).toString().padStart(2, '0');
  return `BRO${timestamp}${random}`;
}
```

2. **QRY API 구현** (조회)
3. **DEL API 구현** (취소)

---

## 7. 결론

### 현재 상태
- ⚠️ **규격서 준수도 약 20%**
- ❌ API 엔드포인트가 규격서와 완전히 다름
- ❌ 요청 형식이 규격서와 불일치
- ❌ 암호화가 적용되지 않음
- ✅ 키 관리 구조는 양호
- ⚠️ 일부 보안 개선 필요

### 권장 조치
1. **즉시 수정**: API 엔드포인트 및 요청 형식을 규격서에 맞게 수정
2. **암호화 적용**: FACE_PRICE, RECV_HPNO 필드 암호화
3. **테스트**: 개발 환경에서 규격서 준수 여부 검증
4. **보안 개선**: Wizzpay 키 클라이언트 노출 방지

---

**분석 완료일**: 2025-01-XX  
**다음 검토 예정일**: 수정 완료 후 재검토

